# Desktop App Scaffold

This directory is reserved for the future Tauri desktop shell.

Planned contents:

- `src-tauri/` for the Rust backend
- a small React UI for quick compare and review-before-send
- clipboard integration
- settings UI for profiles and local runtime selection

Desktop implementation is intentionally deferred until the shared Rust core and
MCP integration are stable.

